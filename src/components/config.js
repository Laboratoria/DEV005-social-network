const firebaseConfig = {
  // Agrega la configuración de tu proyecto de Firebase aquí
};

const twitterApiKey = '...'; // Agrega tu Consumer Key de Twitter aquí
const twitterApiSecretKey = '...'; // Agrega tu Consumer Secret de Twitter aquí

export { firebaseConfig, twitterApiKey, twitterApiSecretKey };
